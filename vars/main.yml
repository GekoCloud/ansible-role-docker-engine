---
# vars file for docker-engine

docker_engine_supported:
  - trusty
  - wily
  - xenial

docker_engine_package_name: 'docker-engine{% if docker_engine_version is defined %}={{ docker_engine_version }}*{% endif %}'
